{
  "name": "ram-peris-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-blog": "node scripts/generate-blog-index.js",
    "sitemap": "node generate-sitemap.js",
    "optimize": "node optimize-images.js",
    "build": "node optimize-images.js && node scripts/sync-agenda-redirects.js && node scripts/generate-blog-index.js && node generate-sitemap.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
